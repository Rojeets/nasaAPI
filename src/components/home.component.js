

export default function Result({ result }) {
    return (
        <>
            <section class="carousel">
                {result ? (
                    result.map((item, index) => {
                        

                    })
                ) : ''}
            </section>

        </>
    )
}